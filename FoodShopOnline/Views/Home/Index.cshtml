@using FoodShopOnline.Common
@model IEnumerable<Model.EnityFramework.Product>
@{
    ViewBag.Title = "Trang chủ";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var slides = (List<Model.EnityFramework.Slide>)ViewBag.Slides;
    var category = (List<Model.EnityFramework.ProductCategory>)ViewBag.category;
    var KM = (List<Model.EnityFramework.Product>)ViewBag.KM;
    var SP = (List<Model.EnityFramework.Product>)ViewBag.SP;
    var KhaiVi = (List<Model.EnityFramework.Product>)ViewBag.KhaiVi;
}
@section jsFooter{

    <script src="~/content/client/controller/LoadMoreProduct.js"></script>
    @*<script src="~/content/client/controller/LoadMoreKhaiVi.js"></script>*@
}
<div id="myCarousel" class="carousel slide" data-ride="carousel">
    <ol class="carousel-indicators">
        <li data-target="#myCarousel" data-slide-to="0" class="active"></li>
        @foreach (var item in slides)
        {
            <li data-target="#myCarousel" data-slide-to="@item.DisplayOrder"></li>
        }
    </ol>
    <div class="carousel-inner">
        <div class="item active">
            <img src="\content\client\images\KM\km4.jpg" style="width:100%; height: 315px">
        </div>
        @foreach (var item in slides)
        {
            <div class="item">
                <a href="@item.Link">
                    <img src="@item.Image" style="width:100%; height: 315px">
                </a>
            </div>
        }

    </div>
    <a class="left carousel-control" href="#myCarousel" data-slide="prev">
        <span class="glyphicon glyphicon-chevron-left"></span>
        <span class="sr-only">Previous</span>
    </a>
    <a class="right carousel-control" href="#myCarousel" data-slide="next">
        <span class="glyphicon glyphicon-chevron-right"></span>
        <span class="sr-only">Next</span>
    </a>
</div>
<div class="pizza">



    <br />

    @*@using (Html.BeginForm("Index", "Home", FormMethod.Get))
        {

            <select name="sort" id="sort">
                <option value="-1">Sắp xếp</option>
                <option value="1">Giá tăng dần</option>
                <option value="-1">Giá giảm dần</option>
            </select>
            <button type="submit"><i class="fas fa-search fa-2x"></i></button>

        }*@
    <p>Khuyến Mãi</p>
    <hr>
    <div class="row">
        <div class="col-md-12">
            @foreach (var x in KM)
            {
                if (x.Status == true)
                {
                    <div class="responsive">
                        <div class="gallery">
                            <a href="/chi-tiet/@x.Alias-@x.ID">
                                <img src="@x.Image">
                            </a>
                            <div class="desc">@x.Name</div>
                            <div class="info">@x.Description</div>
                            <div class="gia">$@x.Price.ToString("N0")</div>
                            <div class="add-actions">
                                <ul class="add-actions-link btn btn-primary" style="background-color: #0062cc">
                                    <li class="add-cart active" style="margin-left: 13px"><a href="/Cart/AddItem?productId=@x.ID&quantity=1" style="font-size: 15px"><span class="glyphicon glyphicon-shopping-cart"></span> THÊM VÀO GIỎ HÀNG</a></li>
                                    @*<li class="add-cart active" style="margin-left: 13px"><a href="/ProductDetails/ProductDetail/@x.ID" style="font-size: 15px"><span class="glyphicon glyphicon-shopping-cart"></span> THÊM VÀO GIỎ HÀNG</a></li>*@
                                </ul>
                            </div>
                        </div>
                    </div>
                }

            }
        </div>
    </div>
    @foreach (var item in Model.Batch(4))
    {

        <p>Thực Đơn &nbsp; &nbsp; &nbsp; </p>
        <hr>
        <p>Pizza &nbsp; &nbsp; &nbsp; </p>
        <div class="row">
            <div class="col-md-12">
                @foreach (var subitem in item)
                {
                    if (subitem.CategoryID == 1 && subitem.Status == true)
                    {
                        <div class="responsive">
                            <div class="gallery">
                                <a href="/chi-tiet/@subitem.Alias-@subitem.ID">
                                    <img src="@subitem.Image">
                                </a>
                                <div class="desc">@subitem.Name</div>
                                <div class="info">@subitem.Description</div>
                                <div class="gia">$@subitem.Price.ToString("N0")</div>
                                <div class="add-actions">
                                    <ul class="add-actions-link btn btn-primary" style="background-color: #0062cc">
                                        <li class="add-cart active" style="margin-left: 13px"><a href="/Cart/AddItem?productId=@subitem.ID&quantity=1" style="font-size: 15px"><span class="glyphicon glyphicon-shopping-cart"></span> THÊM VÀO GIỎ HÀNG</a></li>
                                    </ul>
                                </div>
                            </div>
                        </div>
                    }

                }
            </div>
        </div>
    }
    <br />
    <p id="loadMore" style="text-align: center;">

        <a class="btn btn-outline-primary">
            Xem Thêm +
        </a>

    </p>

    <div class="clearfix"></div>
    <div style="padding:6px;"></div>
</div>
    @*@foreach (var item in Model.Batch(4))
    {
        <hr>
        <p>Khai Vị&nbsp; &nbsp; &nbsp; </p>
        <div class="row">
            <div class="col-md-12">
                @foreach (var subitem in item)
                {
                    if (subitem.CategoryID == 2 && subitem.Status == true)
                    {
                        <div class="responsive">
                            <div class="gallery">
                                <a href="/chi-tiet/@subitem.Alias-@subitem.ID">
                                    <img src="@subitem.Image">
                                </a>
                                <div class="desc">@subitem.Name</div>
                                <div class="info">@subitem.Description</div>
                                <div class="gia">$@subitem.Price.ToString("N0")</div>
                                <div class="add-actions">
                                    <ul class="add-actions-link btn btn-primary" style="background-color: #0062cc">
                                        <li class="add-cart active" style="margin-left: 13px"><a href="/Cart/AddItem?productId=@subitem.ID&quantity=1" style="font-size: 15px"><span class="glyphicon glyphicon-shopping-cart"></span> THÊM VÀO GIỎ HÀNG</a></li>
                                    </ul>
                                </div>
                            </div>
                        </div>
                    }

                }
            </div>
        </div>
    }
    <br />
    <p id="loadMoreKhaiVi" style="text-align: center;">

        <a class="btn btn-outline-primary">
            Xem Thêm +
        </a>

    </p>
</div>*@